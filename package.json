{
  "name": "superflow",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "verify": "chmod +x scripts/verify-local.sh && scripts/verify-local.sh",
    "pre-commit": "npm run format && npm run lint && npm test"
  },
  "devDependencies": {
    "@eslint/js": "^9.33.0",
    "eslint": "^9.33.0",
    "jsdom": "^26.1.0",
    "prettier": "^3.2.5",
    "vitest": "^3.2.4"
  }
}
